import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "#f59e0b",
    padding: 15,
    borderRadius: 8,
    marginTop: 20,
  },

  text: {
    color: "#fff",
    textAlign: "center",
    fontWeight: "bold",
  },
});
