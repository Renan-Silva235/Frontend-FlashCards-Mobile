import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    position: "absolute",
    top: 10,
    right: 10,
    zIndex: 10,
  },

  icon: {
    fontSize: 14,
  },
});
